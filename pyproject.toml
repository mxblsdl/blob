[project]
name = "blob"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "jinja2>=3.1.6",
    "passlib>=1.7.4",
]


[tool.setuptools]
packages = ["app"]
